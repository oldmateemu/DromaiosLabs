// Test stub: the real "server-only" package throws when imported outside an
// RSC bundler. Unit tests exercise these modules directly, so we alias it to
// this no-op in vitest.config.ts.
export {};
